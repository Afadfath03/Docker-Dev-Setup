# CloudBeaver

Web-based database management UI (DBeaver browser version).

## Run

```bash
docker compose up -d
```

## Access

`http://<ip>:8978`

Connect ke database via container name:

| Database | Host |
|---|---|
| MySQL | `mysql-db:3306` |
| PostgreSQL | `postgres-db:5432` |
