# phpMyAdmin

Web-based MySQL/MariaDB admin UI.

## Run

```bash
docker compose up -d
```

## Access

`http://<ip>:8087`

## Connection

Masukkan `mysql-db` sebagai Server (atau host MySQL lain). User `root`, password `admin123`.
