<?php
// PrivateBin Configuration
// Copy this file to conf.php and uncomment the volume mount in docker-compose.yml
// Full reference: https://github.com/PrivateBin/PrivateBin/wiki/Configuration

return [
    'main' => [
        // Nama instance — muncul di browser title & page header
        'name' => getenv('INSTANCE_NAME') ?: 'PrivateBin',

        // --- Security & Privacy ---

        // Nonaktifkan discussion threads — cegah diskusi publik pada paste
        'discussion' => false,
        'opendiscussion' => false,

        // Wajibkan password untuk membuat paste baru
        'password' => true,

        // Nonaktifkan file upload
        'fileupload' => false,

        // Izinkan burn-after-reading (paste hancur setelah dibaca)
        'burnafterreadingselected' => true,

        // Default formatter: syntax highlighting
        'defaultformatter' => 'syntaxhighlighting',
        'syntaxhighlightingtheme' => 'dark',

        // Maksimal ukuran paste: 10 MB
        'sizelimit' => 10485760,

        // Template: bootstrap-dark
        'template' => 'bootstrap-dark',

        // Pesan info di footer
        'info' => 'Zero-knowledge pastebin — server cannot read your data.',

        // Content Security Policy — ketat
        'cspheader' => "default-src 'none'; base-uri 'self'; form-action 'none'; manifest-src 'self'; connect-src * blob:; script-src 'self' 'wasm-unsafe-eval'; style-src 'self'; font-src 'self'; frame-ancestors 'none'; frame-src blob:; img-src 'self' data: blob:; media-src blob:; object-src blob:; sandbox allow-same-origin allow-scripts allow-forms allow-modals allow-downloads",

        // Rate limiting — 10 detik antar request dari IP yang sama
        'traffic_limit' => 10,

        // Purge expired pastes — limit 300, batch 10
        'purge_limit' => 300,
        'purge_batchsize' => 10,
    ],

    'expire' => [
        // Default expiration
        'default' => '1week',
        // Maksimum — cegah paste abadi
        'max' => '1month',
    ],

    'expire_options' => [
        '5min'   => 300,
        '10min'  => 600,
        '1hour'  => 3600,
        '1day'   => 86400,
        '1week'  => 604800,
        '1month' => 2592000,
        // Hapus komentar untuk izinkan paste abadi (tidak disarankan):
        // 'never'  => 0,
    ],

    'formatter_options' => [
        'plaintext'          => 'Plain Text',
        'syntaxhighlighting' => 'Source Code',
        'markdown'           => 'Markdown',
    ],

    'model' => [
        'class' => 'Filesystem',
    ],

    'model_options' => [
        'dir' => '/srv/data',
    ],

    // --- Opsional: Gunakan database sebagai backend (ganti Filesystem) ---
    // Hapus komentar & isi sesuai DB yang dipakai

    // MySQL:
    // 'model' => [
    //     'class' => 'Database',
    // ],
    // 'model_options' => [
    //     'dsn' => 'mysql:host=mysql;dbname=privatebin;charset=UTF8',
    //     'tbl' => 'privatebin_',
    //     'usr' => 'privatebin',
    //     'pwd' => 'change-this-password',
    // ],

    // PostgreSQL:
    // 'model' => [
    //     'class' => 'Database',
    // ],
    // 'model_options' => [
    //     'dsn' => 'pgsql:host=postgres;dbname=privatebin',
    //     'tbl' => 'privatebin_',
    //     'usr' => 'privatebin',
    //     'pwd' => 'change-this-password',
    // ],

    // SQLite:
    // 'model' => [
    //     'class' => 'Database',
    // ],
    // 'model_options' => [
    //     'dsn'  => 'sqlite:/srv/data/db.sq3',
    //     'usr'  => null,
    //     'pwd'  => null,
    // ],

    // --- Opsional: URL shortener ---
    // Gunakan Shlink atau YOURLS untuk memperpendek URL paste
    // Perhatikan: ini akan membocorkan encryption key ke shortener
    // 'urlshortener' => 'https://shortener.example.com/api?link=',
];
