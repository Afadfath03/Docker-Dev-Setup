# PrivateBin Environment Configuration
# Copy this file to .env and adjust values before running docker compose up
#
# Only INSTANCE_NAME is used by conf.php (if enabled).
# All other configuration goes in conf.php, not here.

# Instance name — appears in browser title & page header
INSTANCE_NAME=PrivateBin
